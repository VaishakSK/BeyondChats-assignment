{
  "name": "beyondchats-assignment",
  "version": "1.0.0",
  "description": "BeyondChats Article Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install",
    "install:backend": "npm install --workspace=backend",
    "install:frontend": "npm install --workspace=frontend",
    "install:task3": "npm install --workspace=task3",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build:frontend": "npm run build --workspace=frontend",
    "start:backend": "npm start --workspace=backend"
  },
  "workspaces": [
    "backend",
    "frontend",
    "task3"
  ],
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}

